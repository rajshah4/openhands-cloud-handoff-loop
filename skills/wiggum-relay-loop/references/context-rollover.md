# Context Rollover

Use proactive rollover instead of depending on condensation.

Rollover at:

- a completed bounded step
- a configured turn threshold
- a configured file-review threshold
- a configured note-size threshold

Store these thresholds in repo files so a fresh conversation can make the same decision without hidden memory.
